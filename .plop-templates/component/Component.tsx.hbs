import { ComponentPropsWithRef } from 'react';

import { clsx } from 'clsx';
import styles from './{{properCase componentName}}.module.scss';

interface {{properCase componentName}}Props extends ComponentPropsWithRef<'div'> {}

export const {{properCase componentName}} = ({ className, ...rest }: {{properCase componentName}}Props) => {
  const rootClassName = clsx(className, styles.{{properCase componentName}});

  return (
    <div className={rootClassName} {...rest} />
  );
};
